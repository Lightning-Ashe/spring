'use strict';

var SpringApp = {};

var App = angular.module('SpringApp', ['SpringApp.filters', 'SpringApp.services', 'SpringApp.directives']);

// Declare app level module which depends on filters, and services
App.config(['$routeProvider', function ($routeProvider) {

}]);
